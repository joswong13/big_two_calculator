import 'package:flutter/material.dart';

class RoundData extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Round Data"),
      ),
    );
  }
}
